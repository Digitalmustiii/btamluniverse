// app/(main)/page.tsx
export default function HomePage() {
  return (
    <div>
      {/* This page intentionally left minimal since the layout handles all the main content */}
      {/* All main sections (Header, AfricaThumbnail, BusinessThumbnail, etc.) are rendered in layout.tsx */}
    </div>
  )
}